using System.Linq;
using UnityEngine;
using UnityEngine.SceneManagement;

public class MiniGameManager : MonoBehaviour
{

  /*  private int completedMiniGames = 0;
    private int totalMiniGames;

    void Start()
    {
        // Buscar todos los objetos con tag "MiniGame"
        totalMiniGames = GameObject.FindGameObjectsWithTag("MiniGame").Length;

    }

    public void MiniGameCompleted()
    {

        completedMiniGames++;

        Transform nextLevelButton = transform.Find("NextLevelButton"); // el sprite que se usara como boton
        Transform nextLevelBackground = transform.Find("NextLevelBackground");  // el sprite de victoria (nivel completado)

        if (completedMiniGames >= totalMiniGames)
        {
            if (nextLevelButton != null) 
                nextLevelButton.gameObject.SetActive(true);

            if (nextLevelBackground != null)
                nextLevelBackground.gameObject.SetActive(true);
        }
    }*/

}
